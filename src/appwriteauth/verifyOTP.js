// const verifyOTP = async (userId, secret) => {
//   try {
//     await account.updateVerification(userId, secret);
//     console.log('User verified');
//   } catch (error) {
//     console.error('Error during OTP verification:', error);
//     throw error;
//   }
// };

// export default verifyOTP;
